object Sum_of_digits  extends App {
  val number = 5
  var res = 1
  var sum =0
  for (i <- 1 to number) {
    res = res * i
  }


  /*println(Sum(res))
  def Sum(res: Int): Int =
  {
   if(res <=0 )
     return
  }
  */
}
